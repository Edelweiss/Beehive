{{ index.type }}: {{ index.topic }} {{ index.tab }}: <span id="index_{{ index.id }}_phrase" class="editIndex greek"><sup>{{ index.papyNew ? '*' : '' }}{{ index.greekNew ? '✝' : '' }}</sup>{{ index.phrase }}</span><span id="delete_index_{{ index.id }}" class="action deleteIndex">Indexeintrag löschen</span>